{{!-- 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>
    {{#with main}}
       <main>
        <section>
            <div class="p-2">{{> title data="Work history" big=true}}</div>
            <div>
                <p>
                 <ul>
                   {{#each works}}
                    <li>
                     <div class="pb-3">{{position}}</div>

                     <div class="pb-3">{{duration.from}} - 
                        {{#if duration.to }}
                            {{duration.to}} 
                        {{else}}
                            (досі тут працємо) 
                        {{/if}}
                     </div>

                     <div>{{> link text=compane.name href=compane.url blank=true noLine=true}}</div>
                    
                     
                    <div class="pt-5 pb-3">
                        <strong>Projects ({{projectAmount}})</strong>
                    </div>

                    <ul>
                     {{#each projects}}
                      <li>
                       <div>{{> link text=name href=url blank=true noLine=true}}</div>

                      <p>
                       <div>{{about}}</div>
                      </p>

                      <div><em>Projects stacks ({{stackAmount}})</em></div>
                      <div>
                        <ul>{{#each stacks}}
                            <li>{{name}}</li>
                            {{/each}}
                        </ul>
                     </div>
                       
                        <div class="pt-3"><em>Projects awards</em></div>
                          <div>
                           <ul>{{#each awards}}
                            <li>{{name}}</li>
                               {{/each}}
                           </ul>    
                          </div>
                        </li>
                     {{/each}}
                    </ul>
                    </li>
                   {{/each}}
                 </ul>
                </p>
            </div>
        </section>
    </main>   
    {{/with}}
</div>

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}
