 {{#with main}}
 <main>
   <section>
    {{> title data="My educaptions" big=true}}
    <div>
      <ul>
          {{#each educations}}
            <li>
              {{name}}

              {{#unless isEnd includeZero=true}}(ще в процесі){{/unless}}
            </li>
          {{/each}}
         </ul>
      </div>
   </section>
   <section>
    {{> title data='My sertificates' }}
    <div>
      <ul>
        {{#each sertificates}}
        <li>{{name}}({{id}})</li>
          {{/each}}
        </ul>
      </div>
   </section>
 </main>
{{/with}}